describe('myApp.open-hero-card', function () {
    beforeEach(module('myApp.open-hero-card'));
    describe('OpenCardHeroCtrl controller', function () {
        it('should ....', inject(function ($controller) {
            //spec body
            let OpenCardHeroCtrl = $controller('OpenCardHeroCtrl');
            console.log(OpenCardHeroCtrl);
            expect(OpenCardHeroCtrl).toBeDefined();
        }));

    });
});